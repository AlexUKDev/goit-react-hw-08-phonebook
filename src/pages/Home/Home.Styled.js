import styled from 'styled-components';

export const Wrapper = styled.div`
  padding: 20px;
  width: 70%;
  border: 1px solid rgba(0, 0, 0, 0.2);

  background-color: rgba(84, 185, 95, 0.2);

  border-radius: 10px;

  margin-right: auto;
  margin-left: auto;
  margin-top: 50px;
`;

export const Title = styled.h1`
  margin: 0;
  padding: 0;
  font-weight: 700;
  font-size: 40px;
  text-align: center;
`;

export const Message = styled.p`
  display: inline-block;
  text-align: center;
  font-size: 30px;
  font-weight: 500;
`;
